export interface ImpactStat {
  id: string;
  title: string;
  value: string;
  description: string;
  icon: string;
}

export interface Feature {
  id: string;
  title: string;
  description: string;
  icon: string;
}

export interface WorkStep {
  id: string;
  step: number;
  title: string;
  description: string;
  icon: string;
}
